/* ── tokens ───────────────────────────────────────────────── */
:root {
  --bg: #0b0f0c;
  --surface: #141a16;
  --surface-2: #1a221c;
  --surface-3: #222b25;
  --line: #243028;
  --line-2: #2d3a31;
  --text: #ecf2ee;
  --text-2: #a8b6ad;
  --text-3: #6c7c72;
  --accent: #1ed760;
  --accent-ink: #0b0f0c;
  --danger: #ff6b6b;
  --warn: #f5b942;
  --radius-xl: 24px;
  --radius-lg: 18px;
  --radius-md: 12px;
  --radius-sm: 8px;
  --glow: 0 24px 80px -32px rgba(30,215,96,.35), 0 8px 24px -16px rgba(0,0,0,.6);
}
[data-theme="light"] {
  --bg: #f4f3ee;
  --surface: #ffffff;
  --surface-2: #f9f8f3;
  --surface-3: #eeede6;
  --line: #e3e1d8;
  --line-2: #d4d1c4;
  --text: #141610;
  --text-2: #565c52;
  --text-3: #8a9086;
  --accent: #16a34a;
  --accent-ink: #ffffff;
  --glow: 0 16px 48px -24px rgba(22,163,74,.25), 0 4px 16px -8px rgba(0,0,0,.07);
}

/* ── base ──────────────────────────────────────────────────── */
*, *::before, *::after { box-sizing: border-box; }
html, body {
  margin: 0; padding: 0;
  background: var(--bg); color: var(--text);
  font-family: 'Geist', system-ui, sans-serif;
  font-feature-settings: "ss01","cv11";
  -webkit-font-smoothing: antialiased;
  min-height: 100vh;
}
.mono { font-family: 'Geist Mono', ui-monospace, monospace; }

/* ── app shell ─────────────────────────────────────────────── */
.app {
  min-height: 100vh;
  display: grid; grid-template-rows: auto 1fr auto;
  background:
    radial-gradient(1200px 600px at 50% -8%, color-mix(in oklab,var(--accent) 10%,transparent), transparent 55%),
    var(--bg);
}

/* ── topbar ────────────────────────────────────────────────── */
.topbar {
  display: flex; align-items: center; justify-content: space-between;
  padding: 16px 28px;
  position: sticky; top: 0; z-index: 20;
  backdrop-filter: blur(10px);
  border-bottom: 1px solid color-mix(in oklab,var(--line) 60%,transparent);
}
.brand { display: flex; align-items: center; gap: 10px; font-weight: 600; font-size: 17px; letter-spacing: -.02em; }
.brand-logo {
  width: 30px; height: 30px; border-radius: 9px;
  background: linear-gradient(135deg, var(--accent), color-mix(in oklab,var(--accent) 55%,#fff));
  display: grid; place-items: center; color: var(--accent-ink);
  box-shadow: 0 4px 14px -6px color-mix(in oklab,var(--accent) 55%,transparent);
}
.brand-logo svg { width: 16px; height: 16px; }
.brand-meta { color: var(--text-3); font-size: 11.5px; font-weight: 500; margin-left: 2px; }
.top-actions { display: flex; gap: 6px; }
.icon-btn {
  width: 36px; height: 36px; border-radius: 10px;
  background: transparent; border: 1px solid var(--line);
  color: var(--text-2); cursor: pointer;
  display: grid; place-items: center; transition: all .15s;
}
.icon-btn:hover { background: var(--surface-2); color: var(--text); border-color: var(--line-2); }
.icon-btn svg { width: 16px; height: 16px; }

/* ── main grid ─────────────────────────────────────────────── */
main {
  padding: 28px;
  display: grid;
  place-items: start center;
}
.dashboard {
  width: 100%; max-width: 1080px;
  display: grid;
  grid-template-columns: 1fr 300px;
  grid-template-rows: auto auto;
  gap: 16px;
  align-items: start;
}
/* full-width row for advanced link */
.dashboard > .adv-trigger { grid-column: 1 / -1; }

/* ── card ──────────────────────────────────────────────────── */
.card {
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: var(--radius-xl);
  overflow: hidden;
  box-shadow: var(--glow);
}

/* ── hero ──────────────────────────────────────────────────── */
.hero {
  padding: 24px 26px 20px;
  display: flex; align-items: center; justify-content: space-between; gap: 14px;
  border-bottom: 1px solid var(--line);
  background: radial-gradient(500px 120px at 0 0, color-mix(in oklab,var(--accent) 9%,transparent), transparent 60%);
}
.device-name { font-weight: 600; font-size: 20px; letter-spacing: -.02em; margin-bottom: 6px; }
.pill {
  display: inline-flex; align-items: center; gap: 7px;
  padding: 5px 10px 5px 8px; border-radius: 999px;
  background: var(--surface-2); border: 1px solid var(--line);
  color: var(--text-2); font-size: 12.5px; font-weight: 500;
}
.pill-dot { width: 7px; height: 7px; border-radius: 50%; background: var(--text-3); }
.pill[data-state="on"] { color: var(--text); border-color: color-mix(in oklab,var(--accent) 35%,var(--line)); }
.pill[data-state="on"] .pill-dot {
  background: var(--accent);
  animation: pulse 2s ease-in-out infinite;
}
@keyframes pulse {
  0%,100% { box-shadow: 0 0 0 0 color-mix(in oklab,var(--accent) 45%,transparent); }
  50% { box-shadow: 0 0 0 5px color-mix(in oklab,var(--accent) 0%,transparent); }
}
.power {
  display: inline-flex; align-items: center; gap: 10px; cursor: pointer;
  padding: 5px 12px 5px 5px; border-radius: 999px;
  background: var(--surface-2); border: 1px solid var(--line);
  transition: all .2s; user-select: none;
}
.power:hover { border-color: var(--line-2); }
.power-knob {
  width: 28px; height: 28px; border-radius: 50%;
  background: var(--surface-3); display: grid; place-items: center;
  color: var(--text-3); transition: all .2s;
}
.power[data-on="true"] { background: color-mix(in oklab,var(--accent) 18%,var(--surface-2)); border-color: color-mix(in oklab,var(--accent) 42%,var(--line)); }
.power[data-on="true"] .power-knob { background: var(--accent); color: var(--accent-ink); }
.power-label { font-size: 13.5px; font-weight: 500; color: var(--text-2); }
.power[data-on="true"] .power-label { color: var(--text); }

/* ── now playing ───────────────────────────────────────────── */
.now {
  padding: 24px 26px;
  display: grid; grid-template-columns: auto 1fr;
  gap: 20px; align-items: center;
}
.art {
  width: 140px; height: 140px; border-radius: var(--radius-lg);
  background: linear-gradient(135deg,#2a3530,#1a221c);
  position: relative; overflow: hidden; flex: none;
  box-shadow: 0 10px 28px -12px rgba(0,0,0,.5);
}
.art.has-art { background: linear-gradient(160deg,#ff8a4c 0%,#ff4d6d 50%,#6b2eb8 100%); }
.art::after {
  content: ""; position: absolute; inset: 0;
  background: radial-gradient(circle at 30% 30%,rgba(255,255,255,.14),transparent 55%);
  pointer-events: none;
}
.art-icon { position: absolute; inset: 0; display: grid; place-items: center; color: var(--text-3); opacity: .45; }
.art-icon svg { width: 48px; height: 48px; }
.art.has-art .art-icon { display: none; }
.now-meta { min-width: 0; }
.now-eyebrow {
  display: inline-flex; align-items: center; gap: 7px;
  font-size: 10.5px; font-weight: 600; letter-spacing: .08em; text-transform: uppercase;
  color: var(--accent); margin-bottom: 8px;
}
.bars { display: inline-flex; gap: 2px; align-items: flex-end; height: 9px; }
.bars span { width: 2px; background: var(--accent); border-radius: 1px; animation: bar 1s ease-in-out infinite; }
.bars span:nth-child(1) { height: 55%; animation-delay: 0s; }
.bars span:nth-child(2) { height: 100%; animation-delay: .2s; }
.bars span:nth-child(3) { height: 38%; animation-delay: .4s; }
.bars span:nth-child(4) { height: 75%; animation-delay: .1s; }
@keyframes bar { 0%,100% { transform: scaleY(.35); } 50% { transform: scaleY(1); } }
.track-title { font-size: 21px; font-weight: 600; letter-spacing: -.02em; margin: 0 0 3px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.track-artist { font-size: 14px; color: var(--text-2); margin-bottom: 2px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.track-album { font-size: 12.5px; color: var(--text-3); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.idle-help {
  display: flex; align-items: flex-start; gap: 10px;
  margin-top: 12px; padding: 11px 13px;
  background: var(--surface-2); border: 1px dashed var(--line-2); border-radius: var(--radius-md);
  color: var(--text-2); font-size: 13px; line-height: 1.5;
}
.idle-help b { color: var(--text); font-weight: 600; }
.idle-help svg { flex: none; width: 16px; height: 16px; color: var(--accent); margin-top: 2px; }

/* ── controls ──────────────────────────────────────────────── */
.controls {
  padding: 18px 26px 22px;
  border-top: 1px solid var(--line);
  display: grid; gap: 14px;
}
.ctrl-row { display: grid; grid-template-columns: 36px 1fr auto; gap: 14px; align-items: center; }
.ctrl-icon {
  width: 36px; height: 36px; border-radius: 11px;
  background: var(--surface-2); color: var(--text-2);
  display: grid; place-items: center;
}
.ctrl-icon svg { width: 16px; height: 16px; }
.vol-slider {
  -webkit-appearance: none; appearance: none;
  width: 100%; height: 7px;
  background: var(--surface-3); border-radius: 999px; outline: none;
  background-image: linear-gradient(var(--accent),var(--accent));
  background-repeat: no-repeat; background-size: var(--fill,50%) 100%;
}
.vol-slider::-webkit-slider-thumb {
  -webkit-appearance: none; appearance: none;
  width: 18px; height: 18px; border-radius: 50%;
  background: #fff; border: 3px solid var(--accent); cursor: pointer;
  box-shadow: 0 2px 8px rgba(0,0,0,.2);
}
.vol-slider::-moz-range-thumb { width: 18px; height: 18px; border-radius: 50%; background: #fff; border: 3px solid var(--accent); cursor: pointer; }
.vol-val { font-variant-numeric: tabular-nums; font-size: 13.5px; font-weight: 500; color: var(--text-2); min-width: 32px; text-align: right; }
.output {
  width: 100%; display: flex; align-items: center; gap: 12px;
  padding: 11px 15px; background: var(--surface-2); border: 1px solid var(--line);
  border-radius: var(--radius-md); color: var(--text); cursor: pointer; font: inherit; text-align: left;
  transition: all .15s;
}
.output:hover { border-color: var(--line-2); background: var(--surface-3); }
.output-meta { flex: 1; min-width: 0; }
.output-eyebrow { font-size: 10.5px; text-transform: uppercase; letter-spacing: .08em; color: var(--text-3); font-weight: 600; }
.output-name { font-size: 14px; font-weight: 500; margin-top: 1px; }
.output-chev { color: var(--text-3); }

/* ── right sidebar ─────────────────────────────────────────── */
.sidebar-stack { display: grid; gap: 12px; }
.side-card {
  background: var(--surface); border: 1px solid var(--line); border-radius: var(--radius-lg);
  overflow: hidden;
}
.side-head {
  padding: 14px 16px 12px;
  font-size: 11px; font-weight: 600; letter-spacing: .07em; text-transform: uppercase;
  color: var(--text-3); border-bottom: 1px solid var(--line);
}
.stat-grid { padding: 12px 16px; display: grid; gap: 8px; }
.stat-row { display: flex; align-items: center; justify-content: space-between; gap: 8px; }
.stat-label { display: flex; align-items: center; gap: 7px; font-size: 12.5px; color: var(--text-3); }
.stat-label svg { width: 13px; height: 13px; }
.stat-val { font-size: 13px; font-weight: 500; color: var(--text-2); font-variant-numeric: tabular-nums; }
.stat-val.ok { color: var(--accent); }
.stat-val.warn { color: var(--warn); }
.stat-val.danger { color: var(--danger); }
.stat-divider { height: 1px; background: var(--line); margin: 4px 0; }

.quick-grid { padding: 12px; display: grid; grid-template-columns: 1fr 1fr; gap: 6px; }
.quick-btn {
  display: flex; flex-direction: column; align-items: center; gap: 6px;
  padding: 12px 8px; border-radius: 10px;
  background: var(--surface-2); border: 1px solid var(--line); color: var(--text-2);
  font: inherit; font-size: 12px; font-weight: 500; cursor: pointer;
  transition: all .15s;
}
.quick-btn:hover { background: var(--surface-3); border-color: var(--line-2); color: var(--text); }
.quick-btn svg { width: 20px; height: 20px; }
.quick-btn.danger { color: var(--danger); }
.quick-btn.danger:hover { background: color-mix(in oklab,var(--danger) 10%,var(--surface-2)); border-color: color-mix(in oklab,var(--danger) 30%,var(--line)); }
.quick-btn.primary { color: var(--accent); }
.quick-btn.primary:hover { background: color-mix(in oklab,var(--accent) 10%,var(--surface-2)); border-color: color-mix(in oklab,var(--accent) 30%,var(--line)); }

/* ── advanced trigger ──────────────────────────────────────── */
.adv-trigger {
  width: 100%; background: transparent;
  border: 1px solid var(--line); border-radius: var(--radius-lg);
  padding: 14px 18px; display: flex; align-items: center; justify-content: space-between;
  color: var(--text-2); cursor: pointer; font: inherit; transition: all .15s;
}
.adv-trigger:hover { border-color: var(--line-2); background: var(--surface); color: var(--text); }
.adv-trigger-left { display: flex; align-items: center; gap: 12px; }
.adv-icon { width: 30px; height: 30px; border-radius: 9px; background: var(--surface-2); display: grid; place-items: center; color: var(--text-2); }
.adv-icon svg { width: 15px; height: 15px; }
.adv-title { font-size: 13.5px; font-weight: 500; color: var(--text); }
.adv-sub { font-size: 11.5px; color: var(--text-3); margin-top: 1px; }

/* ── output sheet ──────────────────────────────────────────── */
.sheet-back {
  position: fixed; inset: 0; z-index: 50;
  background: rgba(0,0,0,.6); backdrop-filter: blur(4px);
  display: grid; place-items: center; padding: 20px;
  animation: fadein .18s ease;
}
@keyframes fadein { from { opacity: 0; } to { opacity: 1; } }
.sheet {
  width: 100%; max-width: 440px; background: var(--surface);
  border: 1px solid var(--line); border-radius: var(--radius-xl);
  padding: 8px; box-shadow: 0 32px 64px -16px rgba(0,0,0,.5);
}
.sheet-head { padding: 12px 14px 6px; display: flex; justify-content: space-between; align-items: center; }
.sheet-title { font-weight: 600; font-size: 13.5px; }
.sheet-close { background: transparent; border: none; color: var(--text-3); cursor: pointer; padding: 4px; font-size: 15px; }
.sheet-list { display: grid; gap: 2px; }
.sheet-item {
  width: 100%; text-align: left;
  display: grid; grid-template-columns: 34px 1fr auto; gap: 11px; align-items: center;
  padding: 11px 12px; background: transparent; border: none; border-radius: 11px;
  color: var(--text); cursor: pointer; font: inherit; transition: background .12s;
}
.sheet-item:hover { background: var(--surface-2); }
.sheet-item.active { background: color-mix(in oklab,var(--accent) 10%,var(--surface-2)); }
.sheet-icon { width: 34px; height: 34px; border-radius: 9px; background: var(--surface-2); color: var(--text-2); display: grid; place-items: center; }
.sheet-item.active .sheet-icon { background: var(--accent); color: var(--accent-ink); }
.sheet-icon svg { width: 16px; height: 16px; }
.sheet-item-name { font-size: 14px; font-weight: 500; }
.sheet-item-sub { font-size: 11.5px; color: var(--text-3); margin-top: 1px; }
.sheet-check { color: var(--accent); }
.sheet-check svg { width: 16px; height: 16px; }

/* ── setup view ────────────────────────────────────────────── */
.setup-wrap { width: 100%; max-width: 600px; }
.setup-card {
  background: var(--surface); border: 1px solid var(--line); border-radius: var(--radius-xl);
  padding: 40px; text-align: center; box-shadow: var(--glow);
}
.setup-iconbox {
  width: 68px; height: 68px; margin: 0 auto 18px; border-radius: 20px;
  background: color-mix(in oklab,var(--accent) 14%,var(--surface-2));
  display: grid; place-items: center; color: var(--accent);
}
.setup-iconbox svg { width: 30px; height: 30px; }
.setup-h1 { font-size: 26px; font-weight: 600; letter-spacing: -.02em; margin: 0 0 7px; }
.setup-sub { font-size: 15px; color: var(--text-2); margin: 0 0 26px; max-width: 420px; margin-left: auto; margin-right: auto; line-height: 1.5; }
.setup-steps { text-align: left; max-width: 440px; margin: 0 auto 26px; display: grid; gap: 12px; }
.setup-step {
  display: grid; grid-template-columns: 28px 1fr; gap: 12px; align-items: start;
  padding: 13px 15px; background: var(--surface-2); border: 1px solid var(--line); border-radius: var(--radius-md);
}
.step-num {
  width: 24px; height: 24px; border-radius: 50%;
  background: var(--accent); color: var(--accent-ink);
  display: grid; place-items: center; font-size: 12px; font-weight: 600; margin-top: 1px;
}
.step-title { font-weight: 600; font-size: 13.5px; }
.step-body { font-size: 13px; color: var(--text-2); margin-top: 2px; }
.setup-cta {
  display: inline-flex; align-items: center; gap: 7px;
  padding: 11px 18px; border-radius: 999px;
  background: var(--accent); color: var(--accent-ink);
  border: none; font: inherit; font-weight: 600; font-size: 14.5px;
  cursor: pointer; transition: transform .15s;
}
.setup-cta:hover { transform: translateY(-1px); }

/* ── footer ────────────────────────────────────────────────── */
footer {
  padding: 12px 28px 20px;
  max-width: 1080px; width: 100%; margin: 0 auto;
  display: flex; align-items: center; justify-content: space-between; gap: 12px; flex-wrap: wrap;
  color: var(--text-3); font-size: 12px;
}
.foot-info { display: flex; gap: 14px; flex-wrap: wrap; align-items: center; }
.foot-item { display: inline-flex; align-items: center; gap: 5px; }
.foot-item svg { width: 12px; height: 12px; }
.foot-links { display: flex; gap: 2px; }
.foot-link {
  padding: 5px 9px; border-radius: 7px;
  color: var(--text-3); text-decoration: none; font-size: 12px; font-weight: 500;
  transition: all .15s;
}
.foot-link:hover { color: var(--text); background: var(--surface-2); }

/* ── animate in ────────────────────────────────────────────── */
.card, .side-card, .setup-card { animation: rise .35s cubic-bezier(.2,.7,.2,1); }
@keyframes rise { from { opacity: 0; transform: translateY(6px); } to { opacity: 1; transform: none; } }

/* ── responsive ────────────────────────────────────────────── */
@media (max-width: 860px) {
  .dashboard { grid-template-columns: 1fr; }
  .sidebar-stack { grid-template-columns: 1fr 1fr; }
}
@media (max-width: 600px) {
  .sidebar-stack { grid-template-columns: 1fr; }
  .now { grid-template-columns: 1fr; text-align: center; }
  .art { margin: 0 auto; width: 160px; height: 160px; }
  main { padding: 16px; }
  .topbar { padding: 12px 16px; }
  footer { padding: 10px 16px 18px; }
}
